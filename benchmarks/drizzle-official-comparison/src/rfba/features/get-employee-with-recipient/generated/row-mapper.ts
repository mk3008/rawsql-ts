// @generated by Ashiba benchmark scaffold. Do not edit.
// This file is machine-owned and keeps hot row mapping out of the public boundary.

import type { Row } from '../../../../local/sql-contract-mapper';
import type { Employee } from '../boundary';

function mapEmployeeRow(row: Row): Employee {
  return {
    id: row.id as number,
    lastName: row.lastName as string,
    firstName: row.firstName as string | null,
    title: row.title as string,
    titleOfCourtesy: row.titleOfCourtesy as string,
    birthDate: row.birthDate as string,
    hireDate: row.hireDate as string,
    address: row.address as string,
    city: row.city as string,
    postalCode: row.postalCode as string,
    country: row.country as string,
    homePhone: row.homePhone as string,
    extension: row.extension as number,
    notes: row.notes as string,
    recipientId: row.recipientId as number | null,
  };
}

function mapRecipientRow(row: Row): Employee {
  return {
    id: row.recipient_id as number,
    lastName: row.recipient_lastName as string,
    firstName: row.recipient_firstName as string | null,
    title: row.recipient_title as string,
    titleOfCourtesy: row.recipient_titleOfCourtesy as string,
    birthDate: row.recipient_birthDate as string,
    hireDate: row.recipient_hireDate as string,
    address: row.recipient_address as string,
    city: row.recipient_city as string,
    postalCode: row.recipient_postalCode as string,
    country: row.recipient_country as string,
    homePhone: row.recipient_homePhone as string,
    extension: row.recipient_extension as number,
    notes: row.recipient_notes as string,
    recipientId: row.recipient_recipientId as number | null,
  };
}

export function mapEmployeeWithRecipientRowsToResult(rows: Row[]): Employee[] {
  const mapped = new Array<Employee>(rows.length);
  for (let index = 0; index < rows.length; index += 1) {
    const row = rows[index];
    const employee = mapEmployeeRow(row);
    employee.recipient = row.recipient_id === null ? null : mapRecipientRow(row);
    mapped[index] = employee;
  }
  return mapped;
}
