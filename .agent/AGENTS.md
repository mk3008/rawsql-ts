# Repository Policy Pointer

The repository root `../AGENTS.md` is the canonical policy for this tree. Read it before acting; this file does not duplicate or weaken those rules.

Detailed workflow sources live under `../.codex/guidance/`, Codex-native custom-agent adapters live under `../.codex/agents/`, and repeatable repository skills live under `../.agents/skills/`.

Use `../.agents/skills/rawsql-task-orchestrator/SKILL.md` before acting on a new task, including review-only work, when it needs impact, difficulty, model, reasoning, or worktree routing.
