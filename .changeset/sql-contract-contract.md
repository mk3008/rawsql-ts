---
'@rawsql-ts/sql-contract': minor
---

Merge the mapper and writer cores into the new `@rawsql-ts/sql-contract` package so clients can import both mapper helpers and writer helpers from a single contract package. Update docs/tests to reference the merged entry point.
