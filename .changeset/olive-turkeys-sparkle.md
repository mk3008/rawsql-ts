---
'@rawsql-ts/testkit-core': minor
---

Add `createCatalogRewriter()` so fixture-backed SELECT rewrites can plug directly into `@rawsql-ts/sql-contract` `CatalogExecutor` pipelines without hand-written adapters.
