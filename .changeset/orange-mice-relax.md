---
"@rawsql-ts/ztd-cli": minor
---

`ztd query uses` no longer accepts the deprecated `--specs-dir` alias. Use `--scope-dir` when you need to narrow the project-wide QuerySpec scan to one feature or subtree.
