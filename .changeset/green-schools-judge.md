---
'@rawsql-ts/ztd-cli': minor
---

Add agent-first CLI improvements to `ztd-cli`, including a global `--output json` mode, a new `describe` command, dry-run support for write-capable commands, JSON payload input paths, and stricter input hardening for file paths and identifiers.
