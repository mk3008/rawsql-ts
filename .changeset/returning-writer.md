---
"@rawsql-ts/sql-contract": minor
---

Document and expose an optional `returning` setting on the writer helpers so callers can append deterministic `RETURNING` clauses without altering placeholder numbering.
