---
"rawsql-ts": patch
---

Add `newline: "space"` as a named formatter option and restore the demo `OneLiner` style preset to produce fully single-line SQL by disabling structural breaks and comment export.
