---
'rawsql-ts': patch
---

Rename the benchmark workspace to `benchmarks/ztd-bench-vs-raw` and add the `pnpm ztd:bench:pg-testkit-mode` helper so the pg-testkit comparison runners keep using the right path.
