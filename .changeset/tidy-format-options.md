---
"rawsql-ts": minor
---

Add `parseSqlFormatterOptions` as a strict validation boundary for external formatter configuration while preserving the existing typed `SqlFormatter` constructor behavior.
