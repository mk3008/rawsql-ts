---
"@rawsql-ts/ztd-cli": patch
---

Clarify the query-unit guidance so scaffold and docs emphasize `1 SQL file / 1 QuerySpec / 1 repository entrypoint / 1 DTO` over `tables/views` as the primary taxonomy.
