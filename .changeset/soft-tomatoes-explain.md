---
"rawsql-ts": minor
"@rawsql-ts/ztd-cli": minor
---

Add SSSQL scaffold and refresh commands, and change `DynamicQueryBuilder` so legacy runtime filter predicates fail fast instead of being injected at runtime. Runtime optional-condition pruning, sort, and paging remain supported.
