---
 @rawsql-ts/sql-contract: minor
---

- Add placeholder style configuration (indexed, question, named) to the writer so callers can match sqlformatter tokens while keeping deterministic numbering, and document that the mapper integration suites now cover Postgres, MySQL, SQL Server, and SQLite against the new driver dependencies.
