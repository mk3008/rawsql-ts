---
'rawsql-ts': minor
---

Add an MVP optional-condition pruning pass for truthful SSSQL-style `WHERE` branches, with explicit opt-in parameter targeting and `null`/`undefined` treated as absent-equivalent for those targets.
