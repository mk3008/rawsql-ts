---
"rawsql-ts": minor
---

API output shape review: add optimized `query` model output and caller-controlled SQL formatting options to condition optimization transformers while preserving existing `result.sql` compatibility.
