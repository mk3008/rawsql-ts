---
"@rawsql-ts/sql-grep-core": minor
"@rawsql-ts/mcp-server": minor
---

Expose canonical query usage kinds and allow `find_query_usage` to filter classified matches before aggregation and output controls.
