---
'@rawsql-ts/testkit-core': minor
---

Introduce the TestkitProvider connection strategy helper so ZTD tests can reuse a shared transactional connection or opt into per-test isolation, and document the new API.

