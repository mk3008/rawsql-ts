---
"rawsql-ts": minor
"@rawsql-ts/investigation-core": minor
"@rawsql-ts/mcp-server": minor
---

Add structured static schema diagnostics in core and expose a local `validate_sql` MCP tool that distinguishes proven ambiguous columns from unresolved ownership and returns unsupported query roots as domain limitations.
