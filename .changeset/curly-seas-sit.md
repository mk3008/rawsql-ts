---
'@rawsql-ts/ztd-cli': patch
---

Default `ztd query uses` to the impact view in docs/help and add `--exclude-generated` to skip `src/catalog/specs/generated` during impact scans when those specs are review-only noise.
