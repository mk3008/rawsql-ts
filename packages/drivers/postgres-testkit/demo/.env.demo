# Set to a Postgres URL if you want to run the physical demo against a real database.
POSTGRES_URL=postgres://user:password@localhost:5432/demo
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=demo
