# Smoke Application

This folder holds the orchestration for the `smoke` sample feature.

- Keep use-case flow here.
- Call into `domain` for business rules.
- Keep persistence details behind the feature boundary.

