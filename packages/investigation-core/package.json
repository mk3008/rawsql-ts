{
  "name": "@rawsql-ts/investigation-core",
  "version": "0.1.0",
  "description": "Deterministic query structure, safe scope slicing, column lineage, and fixture extraction analysis for rawsql-ts.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "prepack": "pnpm run build",
    "build": "pnpm --filter rawsql-ts build && tsc -p tsconfig.json",
    "test": "pnpm run typecheck:test && vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts",
    "typecheck:test": "tsc --noEmit -p tsconfig.tests.json",
    "lint": "eslint src --ext .ts --ignore-pattern \"*.d.ts\" --ignore-pattern \"**/*.test.ts\"",
    "release": "pnpm run lint && pnpm run test && pnpm run build && node -e \"require('fs').mkdirSync('../../tmp', { recursive: true })\" && pnpm pack --out ../../tmp/rawsql-ts-investigation-core.tgz && pnpm publish --access public"
  },
  "keywords": [
    "rawsql-ts",
    "sql",
    "lineage",
    "investigation",
    "fixture"
  ],
  "license": "MIT",
  "author": "msugiura",
  "repository": {
    "type": "git",
    "url": "https://github.com/mk3008/rawsql-ts.git",
    "directory": "packages/investigation-core"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "rawsql-ts": "^0.30.1"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vite": "7.3.5",
    "vitest": "^4.1.8"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
